## 1. 新 tool 落地

- [x] 1.1 在 `backend/app/services/chat_agent/tools.py` 加 `SearchWithTopicPrefilterInput` BaseModel（fields: `topic: str`、`query: str`、`k: int = 5`，description 寫清楚「use this for topical cross-episode questions; prefer over search_across_episodes」）+ async function `_search_with_topic_prefilter(inp, ctx)`：內部 (a) call `episode_finders.find_episodes_by_topic(ctx.db, ctx.show_id, [inp.topic])` 拿候選 episodes (b) 若非空 → `rag.retrieve_hybrid(query, episode_id_filter=[ep.episode_id for ep in candidates], k=inp.k)` (c) 若空 → `rag.retrieve_hybrid(query, k=inp.k)` 無 filter fallback。Return `{"chunks": [...], "prefilter_episode_count": N, "fallback_to_full_pool": bool}`。驗證 = `pytest backend/tests/test_chat_agent_topic_prefilter.py::test_prefilter_scopes_to_candidates + test_prefilter_empty_falls_back + test_envelope_fields_always_populated` 3 case 全綠。Covers: search_with_topic_prefilter SHALL pre-scope retrieval to topic-matching episodes
- [x] 1.2 在同檔 `TOOLS` list 註冊 `search_with_topic_prefilter` ToolSpec（name + description + input_model + callable）；同步把 `search_across_episodes` 的 description 改成「Fallback for free-form questions without a clear topic. For questions spanning a known topic / theme across episodes, prefer `search_with_topic_prefilter(topic, query)` to avoid topic-related-but-wrong-episode chunks dominating the merged pool.」驗證 = `python -c "from app.services.chat_agent.tools import TOOLS; assert len(TOOLS)==12 and any(t.name=='search_with_topic_prefilter' for t in TOOLS) and 'Fallback' in next(t.description for t in TOOLS if t.name=='search_across_episodes')"` 印 OK。Covers: Tool registry exposes eleven callables backed by real services

## 2. Unit test 落地

- [x] 2.1 新增 `backend/tests/test_chat_agent_topic_prefilter.py`：fixture mock `episode_finders.find_episodes_by_topic` + `rag.retrieve_hybrid`，4 case：(a) `test_prefilter_scopes_to_candidates` — topic 找到 2 集 → retrieve_hybrid 被 call 時 episode_id_filter 是那 2 集 + envelope `prefilter_episode_count=2` + `fallback_to_full_pool=False`；(b) `test_prefilter_empty_falls_back` — topic 找到 0 集 → retrieve_hybrid 被 call 時不帶 episode_id_filter + envelope `prefilter_episode_count=0` + `fallback_to_full_pool=True`；(c) `test_envelope_fields_always_populated` — 兩條 path 結果都有 `chunks`/`prefilter_episode_count`/`fallback_to_full_pool` 三個 key；(d) `test_chunk_dict_shape_matches_existing_tools` — 回傳的 chunk dict 跟 `_search_across_episodes` 回傳的 shape 一致（同樣經過 `_chunk_to_dict`）。驗證 = `pytest backend/tests/test_chat_agent_topic_prefilter.py -v` 4 case 全綠 + 既有 `test_chat_agent_*.py` 不破壞。Covers: search_with_topic_prefilter SHALL pre-scope retrieval to topic-matching episodes + Envelope fields are always populated

## 3. Prod 驗證 + case study

- [x] 3.1 backend redeploy 到 Zeabur（純 Python 新 tool 註冊，無 migration），等 RUNNING + smoke `/me` 200。驗證 = `npx zeabur deployment list --service-id 69eb10360da29f05f49a4b0b | grep <commit>` 顯示 RUNNING
- [x] 3.2 用 `run_chat_agent_eval_v2.py` 重跑 cross_episode + 相關 mt 題 (`b20,b21,b22,b23,b29,mt02,mt03,mt04`) 對比 baseline `chat-rag-baseline-2026-05-26-post-mt-fix.md`，看 agent 是否自發採用新 tool + cross_episode chunk_recall_grouped mean 是否上升。驗證 = 新 result JSON 對比 baseline，case study `docs/case-studies/retrieval-cross-episode-episode-prefilter-2026-05-26.md` 含 (a) tool_calls 採用率 table（多少題 agent 用 search_with_topic_prefilter vs search_across_episodes）(b) cross_episode chunk_recall mean 對比 (c) at least 1 個明確 before/after answer 對比範例。Covers: search_with_topic_prefilter SHALL pre-scope retrieval to topic-matching episodes
- [x] 3.3 在 case study 結論段標註：(a) 若 agent 自發採用率 ≥ 50% 且 cross_episode mean 上升 → success（無 follow-up）；(b) 若採用率低（< 30%）但 cross_episode mean 因新 tool 可用而 unchanged → tool description affordance 不夠，提議 follow-up `agent-prompt-prefer-topic-prefilter` 加 SYSTEM_PROMPT hint；(c) 若採用率高但 cross_episode mean 沒上升 → mechanism 對但 retrieve_hybrid 還是輸給別集，提議 follow-up `retrieval-cross-episode-chunk-recovery`。驗證 = case study 結論段含三條判讀規則 + 對應 follow-up 提議

## 4. 觀測 hook

- [x] 4.1 確認 `?debug_trace=true` 回傳 tool_calls 內含新 tool 的 envelope fields (`prefilter_episode_count` / `fallback_to_full_pool`)，這些 field 對 future debug 可見。驗證 = prod 跑一題 cross_episode 帶 debug_trace + `jq '.tool_calls[] | select(.name=="search_with_topic_prefilter") | .result_full'` 看到至少一個 envelope 含這兩個 field

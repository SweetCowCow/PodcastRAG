## Tasks

- [x] 1.1 建 bake-off harness `backend/scripts/answer_model_bakeoff.py`（實作 design D1 bake-off 方法 + D4 量測與成本歸因 + D3 切換機制的 harness 端）：候選模型清單（gpt-4.1 / gpt-5.1 / gemini-2.5-flash / gemini-2.5-pro）+ 子集 item ids 寫死（從 `extended-multi-turn-40.json` 選 b23 跨集 + fact + comprehension + negative/refusal + multi-turn 各 1–2，共 ~8–10 題，清單在檔頭註明）；對每模型先 `UPDATE ai_steps SET model=... WHERE step_key='answer'`、再走 prod API（`https://podcastrag-api.zeabur.app`，e2e admin session + `debug_trace=true`）跑每題完整 agentic pipeline；每題每模型擷取 `tool_calls[0].name`、EP107 是否被引用、各 round prompt/completion token、成本（token × AI Hub 單價，單價常數寫死於檔頭 2026-06-07 值）、答案全文；用既有 `backend/eval` judge 評 factual / chunk_recall_grouped；依 design D5 rollback / 安全邊際以 try/finally 確保結束/中斷都把 `ai_steps.answer.model` 還原起始值。驗證：對單一模型單題 dry-run 能產出完整欄位 dict；跑後 SQL 查 `ai_steps.answer.model` 已還原為起始值（gpt-4o）。
- [x] 2.1 跑完整 bake-off（4 模型 × 子集，實作 design D4 量測與成本歸因的輸出端），輸出彙總表（每模型每題 first_tool / ep107_cited / in_tok / out_tok / cost / factual / chunk_recall + 每模型 total cost）+ 並排答案 dump 落盤到 `backend/scripts/` 或 `/tmp` 並複製進 change 目錄供留存。驗證：4 模型 × 子集全跑完無遺漏、表格與並排答案檔存在、結束後 `ai_steps.answer.model` = gpt-4o（design D5 還原確認）。
- [x] 3.1 把彙總表 + 並排答案交 Jacky，依 design D2 選模準則（硬門檻：b23 first tool = `search_with_topic_prefilter`；品質：factual / chunk_recall 不低於 gpt-4o baseline；性價比：total cost）協助決策。驗證：Jacky 明確選定一個 answer 模型（記錄於 change 內決策註記）。
- [x] 4.1 執行 design D3 切換機制：把 prod DB `ai_steps.answer.model` 切為選定模型（`UPDATE ai_steps SET model='<選定>' WHERE step_key='answer'`，免 redeploy）。驗證：SQL 查 `ai_steps.answer.model` = 選定值。
- [x] 5.1 驗證選定模型在 prod 生效（滿足 spec requirement「Answer-step model must honor forced tool_choice with the full tool spec」）：跑 `backend/scripts/b23_prod_smoke.sh`，確認 agent 第一工具 = `search_with_topic_prefilter` 且回應引用 EP107；並對 bake-off 子集確認選定模型 factual 不低於 gpt-4o baseline。驗證：smoke trace `tool_calls[0].name = search_with_topic_prefilter` + citations 含 EP107；factual 比較表顯示不低於 baseline。
- [x] 6.1 執行 design D6 與 b22 的依賴交接：在本 change 決策註記寫明「b22-cross-episode-topic-routing 已解除 block，可 `/spectra-apply b22-cross-episode-topic-routing` unpark 後跑 task 6 prod smoke 收尾」。本 change 不代為執行 b22 apply。驗證：交接註記存在且指明 b22 下一步與依賴已滿足（answer 模型 honor forced tool_choice）。

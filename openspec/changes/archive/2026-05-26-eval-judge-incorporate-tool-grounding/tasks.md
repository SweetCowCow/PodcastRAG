## 1. Dataset schema v2 落地

- [x] 1.1 寫 v2 JSON Schema 檔 `backend/eval/datasets/_chat_rag_schema_v2.json`，宣告所有 v2 欄位（must/acceptable 兩層、GT 三層、新欄位、turns array）；驗證方式 = `python -m jsonschema -i extended-multi-turn-40.v2.json _chat_rag_schema_v2.json` 對 7 題試水手工 entry 全 pass。Covers: chat-rag golden dataset SHALL use v2 schema with must / acceptable tiered fields. 設計依據: D1（dataset schema v2 採擴展不破壞檔案層級結構）+ Interface / data shape
- [x] 1.2 [P] 寫一次性 migration script `backend/eval/migrations/v1_to_v2_schema.py`：v1 40 題機械轉換、`expected_answer_keywords` DROP、`expected_episode_uuids` → `_must`、`audit_status` 預設 `"pending"`，缺欄位 fail-loud 非 0 exit；驗證 = `pytest backend/tests/test_v1_to_v2_migration.py` 3 case（lossless / fail-loud / 試水 7 題 overlay 後 reviewed）。Covers: chat-rag v1 → v2 migration script SHALL preserve every item without losing data. 設計依據: D5（dataset migration script 採保守策略）+ Failure modes
- [x] 1.3 [P] 從 `docs/case-studies/chat-rag-dataset-audit-2026-05-25.md` 把 7 題試水 v2 entry 手工 overlay 進 migration 後的 `extended-multi-turn-40.v2.json`；驗證 = 7 題的 `audit_status` 全 `"human-verified-2026-05-2X"`、`expected_answer_summary` 非 `"PENDING AUDIT"`、b14 同時有 `_must` + `_either` + `_acceptable` 三層 GT。Covers: chat-rag golden dataset SHALL use v2 schema with must / acceptable tiered fields
- [x] 1.4 一次性 swap：runner 整合測試對 `extended-multi-turn-40.v2.json` 全綠後，覆蓋原 `extended-multi-turn-40.json` + 在 commit message 記錄 schema bump；驗證 = `git log --oneline -1` commit 含「schema_version 2.0」字樣 + `jq '.schema_version' extended-multi-turn-40.json` 回 `"2.0"`。Covers: extended-multi-turn-40 dataset SHALL carry ground_truth_chunk_ids on scored turns

## 2. Grader plugin 4 支落地

- [x] 2.1 [P] 寫 `backend/eval/graders/count_consistency.py`：regex `(\d+)\s*(集|個)|共\s*(\d+)` 抓 answer 數字 vs `enumeration_total`；驗證 = `pytest backend/tests/test_grader_count_consistency.py` 4 case（b11 27≠26 fail / 無數字 pass / `expected_count` 缺 None / 共 26 pass）。Covers: count_consistency grader SHALL detect LLM number hallucination on enumeration items. 設計依據: D7（count_consistency 抓「N 集 / 共 N / N 個」三種 pattern）
- [x] 2.2 [P] 寫 `backend/eval/graders/chunk_recall_grouped.py`：三層 GT (`_must` / `_either` / `_acceptable`) 計算 + 10s window；驗證 = `pytest backend/tests/test_grader_chunk_recall_grouped.py` 4 case（must+either group 全中=1.0 / must miss 半 / either group 任一中算 hit / acceptable 不抬 score 上限）。Covers: chunk_recall_grouped grader SHALL credit GT chunks across must / either / acceptable tiers
- [x] 2.3 [P] 寫 `backend/eval/graders/ordinal_resolution.py`：解析 `carry_from` 字串（`t<N>.enumeration_episodes[<index>] sorted by published_at <DESC|ASC>`）+ 比對 agent tool_call episode_id；驗證 = `pytest backend/tests/test_grader_ordinal_resolution.py` 3 case（mt01 t2 EP55 vs EP131 fail / 同題 correct EP131 = 1.0 / single-turn 回 None）。Covers: ordinal_resolution grader SHALL verify multi-turn ordinal resolves to correct episode
- [x] 2.4 [P] 寫 `backend/eval/graders/answer_contradict_check.py`：呼叫 LLM judge 並讀 `answer_contradict_check` 欄位、與 `factual_correctness` `refusal_appropriateness` share 同次 judge call；驗證 = `pytest backend/tests/test_grader_contradict.py` 3 case（b14 兜兩邊話術 fail / b27 無 directive 回 None / 正常 multi-faceted answer pass）。Covers: answer_contradict_check grader SHALL invoke LLM judge for contradiction detection
- [x] 2.5 寫 plugin discovery loader 在 `backend/scripts/run_chat_agent_eval.py`：scan `backend/eval/graders/*.py`、import 每個 module、call 每個 `grade(item, response)`、None 表示不適用該題；驗證 = 加第 5 個 dummy grader `_test_dummy.py` 後 runner 自動 register、加 unit test `test_runner_plugin_discovery.py`。Covers: runner SHALL load chat-rag graders via plugin discovery. 設計依據: D2（grader 改成 plugin 結構一個指標一個檔）

## 3. LLM judge prompt v2 + judge config 接線

- [x] 3.1 寫 prompt 檔 `backend/eval/prompts/chat_judge_v2.md`：含 rules 段 + 三指標 rubric + ≥2 個 few-shot examples（b14 contradict + b15 alias），靜態 prefix ≥1024 tokens；驗證 = `wc -c backend/eval/prompts/chat_judge_v2.md` 對應 token 估算 + 用 `anthropic.tokenizer` count prefix ≥1024、b14 / b15 範例文字實際出現在檔內。Covers: chat-rag LLM judge prompt SHALL incorporate agent tool I/O for grounding. 設計依據: D3（LLM judge 一次餵全題不分指標多次 call）+ D4（餵 tool I/O 採 truncate + 結構化）+ D6（refusal_appropriateness 子型 refusal_with_correction）
- [x] 3.2 寫 judge call wrapper `backend/eval/judge_chat_v2.py`：build payload（含 truncated 800-char tool result_summary）、call AI Hub OpenAI-compatible endpoint、parse strict JSON、1 次 retry 失敗標 `"error"`；驗證 = `pytest backend/tests/test_judge_chat_v2.py` 3 case（happy path 三指標 JSON、malformed → retry → error、prompt cache prefix 鎖定）。Covers: chat-rag LLM judge prompt SHALL incorporate agent tool I/O for grounding
- [x] 3.3 加 `judge_prompt_sha256` 到 eval report：runner 啟動讀 prompt 檔算 SHA-256、寫進 `backend/eval/results/<run-id>/report.md` 標頭；驗證 = 跑一次 eval 後 `grep "judge_prompt_sha256:" report.md` 回 64-hex + `shasum -a 256 chat_judge_v2.md` 對得上。Covers: judge prompt template SHALL be version-pinned and externalised

## 4. Runner aggregate 改造（per-design_type / per-indicator）

- [x] 4.1 寫 aggregate 函式 `backend/scripts/run_chat_agent_eval.py` 內 `_aggregate_by_design_type_and_indicator(items_results) -> dict`：output shape per spec rag-eval-runner 新 Requirement；驗證 = `pytest backend/tests/test_runner_aggregate_v2.py` 2 case（per-design-type 分組正確、`overall.by_indicator` 不跨指標平均）。Covers: runner aggregate report SHALL report each indicator independently without cross-design_type averaging
- [x] 4.2 寫 markdown report 渲染：每 design_type 一段 table + summary by indicator 一段 table；驗證 = 跑 7 題試水 + check `report.md` 含 `## deep_dive (n=2)` `## negative (n=1)` 等 section + summary table 不出現「overall pass rate」單一數字。Covers: runner aggregate report SHALL report each indicator independently without cross-design_type averaging
- [x] 4.3 既有 semantic-mode `eval_mode` dispatch 邏輯保留不動，runner 用 `schema_version` 判斷走 v1 還 v2 grader 路徑；驗證 = `pytest backend/tests/test_runner_schema_dispatch.py` 2 case（this-not-that-cool.json 走舊路徑 / extended-multi-turn-40.json schema_version=2.0 走 plugin 路徑）。Covers: Recall@K and MRR are computed per query against ground-truth chunks

## 5. 試水 7 題 v2 baseline 落地

- [x] 5.1 跑 v2 grader 對 7 題試水（用 prod backend `https://podcastrag-api.zeabur.app` + E2E backdoor session）：`python backend/scripts/run_chat_agent_eval.py --dataset extended-multi-turn-40.json --filter-ids b22,b27,b29,b11,b15,b14,mt01 --output /tmp/v2-baseline-7.json --judge-model <PRODUCTION_JUDGE_MODEL>`；驗證 = output JSON 含 7 題 + 每題每指標 score 對齊 case study 人工判讀（b22 episode_set_f1<1 / b27 6/6 全綠 / b29 must 1.0 acceptable miss / b11 count_consistency=0 / b15 全綠 + recall@k=1 / b14 contradict=0 + recall=0.67 / mt01 t1 全綠 t2 ordinal=0）。Covers: chat-rag LLM judge prompt SHALL incorporate agent tool I/O for grounding. 設計依據: Behavior + Acceptance criteria
- [x] 5.2 寫 baseline 對比文 `docs/case-studies/chat-rag-dataset-audit-2026-05-26-baseline.md`：含 v1 vs v2 grader 分數對比表 + 三點 insight（substring vs LLM judge 差異、acceptable tier 價值、tool I/O grounding 對 contradict 偵測效益）；驗證 = `wc -l` ≥80 行 + 含 table + 「不是 regression 是 metric 升級」明確說明 + 對 5 個試水洞 的數字佐證。Covers: chat-rag LLM judge prompt SHALL incorporate agent tool I/O for grounding

## 6. BREAKING 相容性掃 + cleanup

- [x] 6.1 grep `expected_answer_keywords` 全 repo：找出所有讀者（除 migration script 自己）；驗證 = `grep -rn "expected_answer_keywords" backend/ docs/ --exclude-dir=migrations --exclude="*audit*.md" --exclude="*case-studies*"` 應只剩零個 active code reference；找到任何 active reference 都寫進 cleanup 子任務。Covers: expected_answer_keywords field SHALL be rejected on v2 items. 設計依據: Scope boundaries
- [x] 6.2 對 admin endpoint `golden_set_freshness`（per spec `golden-set-maintenance`）確認只讀 `audit_status` 跟 `last_modified` 不讀 `expected_*`；驗證 = 讀 `backend/app/api/admin/golden_set.py` 並 unit test `test_admin_golden_set_v2_compat.py` 對 v2 dataset 跑回 200 OK 不爆。Covers: expected_answer_keywords field SHALL be rejected on v2 items
- [x] 6.3 v1 → v2 一次性切換後在 `docs/eval-strategy.md` 加一行「schema v2 上線日期 = 2026-05-XX」+ commit hash 記錄；驗證 = `grep -n "schema v2 上線" docs/eval-strategy.md` 回非空 + hash 對得上 git log。Covers: chat-rag golden dataset SHALL use v2 schema with must / acceptable tiered fields

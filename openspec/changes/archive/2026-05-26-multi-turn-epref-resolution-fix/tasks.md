## 1. Tool dispatcher mechanical fix

- [x] 1.1 在 `backend/app/services/chat_agent/tools.py` 的 `find_episode_by_ref` dispatcher post-processing 加 auto-pin：當 `episode_finders.find_by_ref` return 非 None EpisodeRef 時，寫 `ChatSessionState.focused_episode_id = resolved_episode_id` + `focused_episode_at = now()`、persist 到 Redis、tool result envelope 加 `auto_pinned: true`；resolve None 時 envelope 加 `auto_pinned: false` 且不動 state。驗證 = `pytest backend/tests/test_chat_agent_epref_carry.py::test_find_episode_by_ref_auto_pins` 全綠 + 既有 `test_chat_session_state.py` 不破壞。Covers: find_episode_by_ref SHALL auto-pin resolved episode to session focused state
- [x] 1.2 在 `backend/app/services/chat_agent/tools.py` 的 `search_within_episode` / `get_episode_segments` / `get_episode_summary` 三個 tool dispatcher 加 episode_id 預處理：args.episode_id 非空 → use as-is + envelope `episode_id_source: "explicit"`；args.episode_id 空且 `ChatSessionState.focused_episode_id` 非 None → substitute + envelope `episode_id_source: "session_focused"` + `effective_episode_id: <uuid>`；兩者皆無 → 不 invoke 底層 + envelope `episode_id_source: "missing"` + `user_hint` 指示 LLM call find_episode_by_ref。驗證 = `pytest backend/tests/test_chat_agent_epref_carry.py::test_episode_id_fallback_*` 3 case（explicit / session_focused / missing）全綠。Covers: Episode-scoped tools SHALL fall back to session focused_episode_id when episode_id arg is omitted
- [x] 1.3 `pin_episode` 改 idempotent：當 `ChatSessionState.focused_episode_id == episode_id` → envelope 加 `already_pinned: true` 且 `ok: true`、可 refresh `focused_episode_at`、不 raise error；當 None 或不同 → 維持既有 write 行為 + envelope `already_pinned: false`。驗證 = `pytest backend/tests/test_chat_agent_epref_carry.py::test_pin_episode_idempotent_*` 3 case（already-pinned / different / from-empty）全綠。Covers: pin_episode SHALL be idempotent when target episode is already focused

## 2. 新增測試 file 與 case

- [x] 2.1 新增 `backend/tests/test_chat_agent_epref_carry.py`：fixture 構建 mock `ChatSessionState` + mock `episode_finders.find_by_ref` + mock `rag.retrieve_hybrid`；含 task 1.1-1.3 引用的所有 test case names（共 8 個 case：1.1 兩個 + 1.2 三個 + 1.3 三個）。驗證 = 跑 `pytest backend/tests/test_chat_agent_epref_carry.py -v` 8 個 case 全 PASS

## 3. Prod 驗證 + case study

- [x] 3.1 Backend redeploy 到 Zeabur（純 Python 改動，無 migration），等 RUNNING + service smoke `/me` 200
- [x] 3.2 用 `run_chat_agent_eval_v2.py` 重跑 mt01/mt02/mt03/mt04 四題（PYTHONPATH=. + backend/.env source + 帶 E2E backdoor session）：`python -m backend.scripts.run_chat_agent_eval_v2 --dataset backend/eval/datasets/extended-multi-turn-40.json --filter-ids mt01,mt02,mt03,mt04 --output /tmp/v2-mt-after-fix.json --report /tmp/v2-mt-after-fix.md --judge-model gpt-5-nano`。驗證 = 對比 `/tmp/v2-triage-27.json` 的 mt02/03/04 結果：(a) mt02 t2 tool_calls 含 `search_within_episode(episode_id=<EP143-uuid>)` 取代「請給我 UUID」放棄；(b) mt03 t2 refusal verdict 從 `should_answer` 翻 `appropriate` 且 answer 含 EP140 餐廳推薦；(c) mt04 t2 不再 drift 到 EP112、tool_calls 含 EP19-uuid；(d) mt04 t3 不再編造 EP112+EP46；(e) mt01 t2 ordinal_resolution 仍 fail（本 change 不修，確認 isolation）。Covers: find_episode_by_ref SHALL auto-pin resolved episode to session focused state + Episode-scoped tools SHALL fall back to session focused_episode_id when episode_id arg is omitted
- [x] 3.3 寫 case study `docs/case-studies/multi-turn-epref-resolution-fix-2026-05-26.md`：含 before/after 4 題 turn-by-turn tool_calls 對比表 + 三點 insight（mechanical fix 對 prompt 飽和的避戰、auto_pinned envelope 對未來 prompt tuning 的可觀測性、focused_episode_id 跨 turn carry 在 mt04 drift 問題上的隔離效果）。驗證 = `wc -l docs/case-studies/multi-turn-epref-resolution-fix-2026-05-26.md` ≥ 60 + 含 before/after 對比 table + 顯式說明 mt01 t2 ordinal bug 仍存在（隔離邊界）

## 4. 觀測 hook

- [x] 4.1 確認 `?debug_trace=true` 回傳 tool_calls 內含新加的 envelope fields (`auto_pinned` / `episode_id_source` / `effective_episode_id` / `already_pinned`)，這些 field 對 future debug 可見。驗證 = prod 跑一次 mt03 t2 debug_trace + `jq '.tool_calls[] | {name, args, result_full}' resp.json` 看到至少一個 envelope 有 `episode_id_source: "session_focused"` field

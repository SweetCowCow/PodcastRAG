# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/

# 環境變數（含 API 金鑰，絕對不能上傳）
.env
.env.local
.env.*.local
.env.bak*
*/.env.bak*

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# 暫存與日誌
*.log
tmp/
temp/
.spectra/
openspec/.vector-search.db*

# Claude Code 個人 local 設定（允許清單等），不跨機器同步
.claude/settings.local.json
.claude/worktrees/

# Playwright MCP 測試產物（截圖、console log）
.playwright-mcp/

# Generated presentation deck (not source-controlled)
*.pptx

# Eval framework — local results + DeepEval cache + judge mini-set (not source-controlled per project memory)
backend/eval/results/
backend/eval/datasets/_judge_minisset.json
.deepeval/

# Claude Code internal scheduling state
.claude/scheduled_tasks.lock

# Working notes / case studies (not source-controlled per project memory rules)
docs/case-studies/
docs/runbooks/
docs/research/
docs/roadmap.pdf
docs/bills/

# Content strategy & social posts (drafts, playbooks, tracking — local only, per project memory rules)
docs/content/

# Random screenshots / scratch images at repo root
aisteps-tab.png
backend/eval/runs/

# Local docker-compose overrides (host port remapping, dev-only env vars)
docker-compose.override.yml
docker-compose.override.yaml

# Agent CLI 工具個人快取 / 設定（不跨機器同步）
.agents/
.codex/

# 本地暫存（benchmark embedding cache、截圖、暫存腳本等）
.tmp/

# Golden set / dataset 備份檔（自動產生，不入版控）
*.bak-*

backend/eval/datasets/extended-multi-turn-40.v1.json.bak

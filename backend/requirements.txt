fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic-settings==2.6.1
pgvector==0.3.6
python-dotenv==1.0.1
feedparser==6.0.11
httpx==0.28.1
celery[redis]==5.4.0
redis==5.2.1
boto3==1.35.71
openai>=1.86.0,<2
faster-whisper==1.1.0
cachetools==5.5.0
authlib==1.3.2
itsdangerous==2.2.0
tiktoken==0.8.0
jieba==0.42.1
# Agentic framework bake-off (prototype B). Slim variant + openai provider only.
pydantic-ai-slim[openai]==0.0.55
# Schema validation (used by golden-set dataset tests)
jsonschema==4.23.0
# NOTE: deepeval is intentionally NOT in this file — it pulls in langchain +
# chromadb + many heavy deps that timeout `pip install` in cold-cache GHA
# (>10min). Install separately for eval / judge work:
#   pip install -r requirements-eval.txt
# The runner falls back to retrieval-only mode (--skip-judge) when deepeval
# is missing, so backend tests + production worker don't need it.
